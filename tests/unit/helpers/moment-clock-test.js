
import { momentClock } from 'heisenberg-helper/helpers/moment-clock';
import { module, test } from 'qunit';

module('Unit | Helper | moment clock');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentClock([42]);
  assert.ok(result);
});

