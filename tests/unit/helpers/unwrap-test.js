
import { unwrap } from 'heisenberg-helper/helpers/unwrap';
import { module, test } from 'qunit';

module('Unit | Helper | unwrap');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unwrap([42]);
  assert.ok(result);
});

